---
"slidev-addon-window-mockup": patch
---

Migrate dev tooling from ESLint + Prettier to the Oxc toolchain (`oxlint`
+ `oxfmt`) and enable oxlint's type-aware linting. No changes to the
published runtime behavior.
